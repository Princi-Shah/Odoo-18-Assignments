<odoo>
    <record id="view_product_template_library_book_form" model="ir.ui.view">
        <field name="name">Product Template Library Book Form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/notebook" position="inside">
                <page string="Book Details">
                    <group>
                        <field name="is_library_book"/>
                        <field name="author_ids" required="sale_ok == True"
                               widget="many2many_tags"/>
                        <field name="publisher" required="sale_ok == True"/>
                        <field name="edition" required="sale_ok == True"/>
                        <field name="published_date" required="sale_ok == True"/>
                        <field name="pages" required="sale_ok == True"/>
                        <field name="available" required="sale_ok == True"/>
                        <field name="barcode" string="ISBN Number"/>
                    </group>
                </page>
            </xpath>
            <xpath expr="//field[@name='list_price']" position="attributes">
                <attribute name="readonly">sale_ok</attribute>
            </xpath>
            <xpath expr="//field[@name='barcode']" position="replace">
            </xpath>
        </field>
    </record>
</odoo>
